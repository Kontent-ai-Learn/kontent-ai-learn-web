html(lang='en')
  head
    meta(name='viewport', content='width=device-width, initial-scale=1')
    meta(charset='utf-8')
    title Service check
    meta(name='robots', content='noindex,nofollow')
    link(rel='stylesheet', href=`${urlPathPrefix}/css/service-check.min.css?v=${deployVersion}`)
    script var auth0Config = {domain:"#{process.env['AUTH0_DOMAIN']}",clientID:"#{process.env['AUTH0_CLIENT_ID']}"}
    script(src=`${urlPathPrefix}/js/service-check.min.js?v=${deployVersion}`, defer='')
  body